import { LoginComponent } from "../components/Login/LoginComponent"
import { Layout } from "@repo/ui/styles"

export const LogIn = () => {
  return (
    <Layout>
      <LoginComponent />
    </Layout>
  )
}
